---
title: Introduction
description: Persistent AI familiars. Composable. Observable. Publishable.
---

import { Cards, Card } from 'fumadocs-ui/components/card';
import { DocsDataTable } from '@/components/docs-data-table';
import { GraphView } from '@/components/graph-view';
import { buildGraph } from '@/lib/build-graph';

# Welcome to Coven

**Coven** is the local runtime substrate for the OpenCoven familiar ecosystem. It launches coding-agent harnesses inside explicit project boundaries, records session history, and exposes the same work through a CLI, TUI, and local daemon API.

<Cards>
  <Card title="Getting Started" href="/docs/guide/getting-started" description="Install Coven and launch your first project-scoped harness session." />
  <Card title="Tinkerer's next 30 minutes" href="/docs/guide/tinkerers-next-30-minutes" description="Inspect the daemon API, JSON sessions, events, and fake-harness smoke path." />
  <Card title="Concepts" href="/docs/guide/concepts" description="Understand how Coven works at its core." />
  <Card title="Cast Codes" href="/docs/guide/cast-codes" description="Learn the portable intent grammar for routing familiar work." />
  <Card title="Cast Agent" href="/docs/familiars/cast-agent" description="Understand the CastCodes substrate and session backend." />
  <Card title="Architecture" href="/docs/guide/architecture" description="How Coven is structured under the hood." />
  <Card title="API Reference" href="/docs/reference/api" description="Full API docs and type signatures." />
</Cards>

## What Makes Coven Different?

<DocsDataTable
  caption="Coven differentiators"
  searchPlaceholder="Filter differentiators..."
  columns={[
    { key: 'pillar', label: 'Pillar' },
    { key: 'group', label: 'Group', filter: true },
    { key: 'meaning', label: 'Meaning' },
  ]}
  rows={[
    { pillar: '**Local authority boundary**', group: 'Trust', meaning: 'The Rust daemon validates project roots, harness ids, live-session state, and sensitive actions.' },
    { pillar: '**Harness-neutral sessions**', group: 'Runtime', meaning: 'Codex and Claude Code run through one project-scoped session model with a clean adapter path.' },
    { pillar: '**Observable work**', group: 'Review', meaning: 'Session metadata and append-only events make live and completed work inspectable.' },
    { pillar: '**Client-ready API**', group: 'Integration', meaning: 'CLI, TUI, CastCodes, OpenMeow, comux, and OpenClaw can coordinate through the local socket contract.' },
    { pillar: '**Familiar substrate**', group: 'Ecosystem', meaning: 'Persistent familiars need durable local workspaces, memory surfaces, tools, roles, and handoff points.' },
  ]}
/>

```typescript
// One local daemon, one project boundary, visible harness work.
await coven.sessions.create({
  projectRoot: '/Users/val/projects/demo',
  harness: 'codex',
  prompt: 'summarize this repo in five bullets',
});
```

## Docs Map

<GraphView graph={buildGraph()} />
