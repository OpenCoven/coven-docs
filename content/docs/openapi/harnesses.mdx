---
title: "Harnesses API"
description: "Manage harnesses — the AI providers that power your familiars."
---

import { DocsDataTable } from '@/components/docs-data-table';

# Harnesses

A **harness** is an AI provider adapter that powers one or more familiars. Coven supports multiple harness types: `openclaw`, `claude_code`, `codex`, `hermes`, and custom adapters.

Each familiar is bound to exactly one harness. You can migrate a familiar between harnesses at any time.

## List Harnesses

```http
GET /v1/harnesses
```

```json
{
  "harnesses": [
    {
      "id": "harness_openclaw",
      "name": "OpenClaw",
      "kind": "openclaw",
      "status": "healthy",
      "familiar_count": 3,
      "created_at": "2026-05-01T00:00:00Z"
    },
    {
      "id": "harness_claude_code",
      "name": "Claude Code",
      "kind": "claude_code",
      "status": "healthy",
      "familiar_count": 1,
      "created_at": "2026-05-10T00:00:00Z"
    }
  ]
}
```

## Get a Harness

```http
GET /v1/harnesses/:id
```

```json
{
  "id": "harness_openclaw",
  "name": "OpenClaw",
  "kind": "openclaw",
  "status": "healthy",
  "config": {
    "gateway_url": "wss://...",
    "model_primary": "openai/gpt-5.5",
    "model_fallbacks": ["openai/gpt-5.4-mini"]
  },
  "familiar_count": 3,
  "familiars": ["fam_nova", "fam_sage", "fam_echo"]
}
```

## Register a Harness

```http
POST /v1/harnesses
Content-Type: application/json

{
  "name": "My Codex",
  "kind": "codex",
  "config": {
    "api_key": "sk-...",
    "model_primary": "openai/gpt-5.5"
  }
}
```

<DocsDataTable
  caption="Register a harness request fields"
  searchPlaceholder="Filter fields..."
  preserveOrder
  columns={[
    { key: 'field', label: 'Field' },
    { key: 'type', label: 'Type', filter: true },
    { key: 'required', label: 'Required', filter: true },
    { key: 'description', label: 'Description' },
  ]}
  rows={[
    { field: '`name`', type: 'string', required: 'Yes', description: 'Display name.' },
    { field: '`kind`', type: 'string', required: 'Yes', description: 'One of `openclaw`, `claude_code`, `codex`, `hermes`, or `custom`.' },
    { field: '`config`', type: 'object', required: 'Yes', description: 'Harness-specific configuration.' },
  ]}
/>

## Update a Harness

```http
PATCH /v1/harnesses/:id
Content-Type: application/json

{
  "config": {
    "model_primary": "anthropic/claude-sonnet-4-5",
    "model_fallbacks": ["anthropic/claude-haiku-3.5"]
  }
}
```

## Harness Health Check

```http
GET /v1/harnesses/:id/health
```

```json
{
  "id": "harness_openclaw",
  "status": "healthy",
  "latency_ms": 42,
  "last_check": "2026-05-17T20:00:00Z"
}
```

## List Familiars on a Harness

```http
GET /v1/harnesses/:id/familiars
```

## Deregister a Harness

```http
DELETE /v1/harnesses/:id
```

> Familiars bound to this harness must be migrated first. Returns `409` if active familiars exist.

## Harness Kinds

<DocsDataTable
  caption="Supported harness kinds"
  searchPlaceholder="Filter harness kinds..."
  columns={[
    { key: 'kind', label: 'Kind' },
    { key: 'category', label: 'Category', filter: true },
    { key: 'description', label: 'Description' },
  ]}
  rows={[
    { kind: '`openclaw`', category: 'Gateway', description: 'OpenClaw gateway (default, recommended).' },
    { kind: '`claude_code`', category: 'Interactive PTY', description: 'Anthropic Claude Code interactive PTY.' },
    { kind: '`codex`', category: 'CLI', description: 'OpenAI Codex CLI.' },
    { kind: '`hermes`', category: 'Local runner', description: 'Hermes local runner.' },
    { kind: '`custom`', category: 'Extension', description: 'Custom adapter via OpenCoven SDK.' },
  ]}
/>

import { FamiliarSchemaDiagram } from '@/components/familiar-schema-diagram';

## Familiar–Harness Relationship

<FamiliarSchemaDiagram />
