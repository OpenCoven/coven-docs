---
title: "Cast Codes"
description: "Portable intent grammar for routing familiar work across CastCodes, CLI/TUI, OpenClaw, and chat clients."
---

import { DocsDataTable } from '@/components/docs-data-table';
import { Mermaid } from '@/components/mermaid';

Cast Codes are OpenCoven's portable intent grammar. They give humans, agents, and clients a compact way to express routing intent without making any client the authority boundary.

Cast Codes are **intent**, not enforcement. A client or Cast Agent can parse a Cast Code into a Coven request, but the Rust daemon still validates project roots, working directories, harness ids, live-session state, and action ids before anything sensitive happens.

<Mermaid chart={`
flowchart LR
  Intent[Human or agent intent] --> Grammar[Cast Code grammar]
  Grammar --> Client[Client parser or Cast Agent]
  Client --> Request[Coven session/action/capability request]
  Request --> Daemon[Coven daemon\nRust authority boundary]
  Daemon --> Harness[Harness session or event result]
`} />

## Current Posture

The grammar is being documented as product language and client intent syntax. Treat examples as draft unless a command or endpoint is explicitly documented as shipped.

<DocsDataTable
  caption="Cast Code maturity posture"
  searchPlaceholder="Filter Cast Code posture..."
  columns={[
    { key: 'layer', label: 'Layer' },
    { key: 'status', label: 'Status', filter: true },
    { key: 'meaning', label: 'Meaning' },
  ]}
  rows={[
    { layer: 'Portable syntax', status: 'Direction', meaning: 'The grammar should be usable in CastCodes, CLI/TUI, OpenClaw, chat clients, and future clients.' },
    { layer: 'Client parsing', status: 'Client-owned', meaning: 'A client can parse visible intent and submit a structured request.' },
    { layer: 'Daemon enforcement', status: 'Shipped boundary', meaning: 'Coven daemon validation remains the only authority for sensitive runtime behavior.' },
    { layer: 'Handoff and orchestration commands', status: 'Planned', meaning: 'Document as roadmap behavior until parser/API support exists.' },
  ]}
/>

## Example Glossolalia

<DocsDataTable
  caption="Cast Code examples"
  searchPlaceholder="Filter Cast Code examples..."
  columns={[
    { key: 'code', label: 'Code' },
    { key: 'intent', label: 'Intent', filter: true },
    { key: 'runtimeMapping', label: 'Runtime mapping' },
    { key: 'status', label: 'Status', filter: true },
  ]}
  rows={[
    { code: '`:>sage`', intent: 'Switch familiar', runtimeMapping: 'Select Sage as the active familiar or lane.', status: 'Draft syntax' },
    { code: '`:cody: review this diff`', intent: 'Delegate task', runtimeMapping: 'Create a task for Cody with the quoted prompt.', status: 'Draft syntax' },
    { code: '`:* "new constraint: local-only"`', intent: 'Broadcast context', runtimeMapping: 'Fan out a shared constraint to active familiars or sessions.', status: 'Draft syntax' },
    { code: '`:?`', intent: 'Status', runtimeMapping: 'Read active familiar/session status.', status: 'Nearest to shipped session/status behavior' },
    { code: '`:^sage`', intent: 'Resume', runtimeMapping: 'Resume Sage or the matching prior lane.', status: 'Draft syntax' },
    { code: '`:<echo`', intent: 'Handoff request', runtimeMapping: 'Request a handoff packet for Echo or from Echo.', status: 'Planned orchestration' },
  ]}
/>

## Where Cast Codes Can Appear

<DocsDataTable
  caption="Cast Code surfaces"
  searchPlaceholder="Filter Cast Code surfaces..."
  columns={[
    { key: 'surface', label: 'Surface' },
    { key: 'role', label: 'Role', filter: true },
    { key: 'boundary', label: 'Boundary' },
  ]}
  rows={[
    { surface: 'CastCodes', role: 'First-party workspace', boundary: 'Render and parse Cast Codes as native workspace commands.' },
    { surface: '`coven` CLI/TUI', role: 'Command surface', boundary: 'Accept typed intent and translate to daemon requests when supported.' },
    { surface: 'OpenClaw plugin', role: 'Bridge', boundary: 'Submit structured Coven requests without bypassing daemon validation.' },
    { surface: 'Telegram, Discord, or chat clients', role: 'Channel', boundary: 'Let familiar lanes receive portable routing intent.' },
    { surface: 'Future clients', role: 'Extension point', boundary: 'Speak the same grammar without depending on one app implementation.' },
  ]}
/>

## CastCodes → Coven Demo Loop

This is the public demo path a new technical user should be able to follow: CastCodes captures intent, Coven launches and records the harness session, CastCodes keeps the work visible, and the user reviews the result before any merge or cleanup.

<Mermaid chart={`
flowchart LR
  Step1["Step 1: Open Project"]
  Step2["Step 2: Launch Session"]
  Step3["Step 3: Watch Session"]
  Step4["Step 4: Inspect Files"]
  Step5["Step 5: Finish Loop"]

  Step1 -->|Cast Code intent| Step2
  Step2 -->|POST /api/v1/sessions| Step3
  Step3 -->|events stream| Step4
  Step4 -->|review diffs| Step5
  Step5 -->|archive / summon / sacrifice| Step1
`}
/>

<DocsDataTable
  caption="CastCodes to Coven demo steps"
  searchPlaceholder="Filter demo steps..."
  columns={[
    { key: 'step', label: 'Step' },
    { key: 'userAction', label: 'User action' },
    { key: 'runtimePath', label: 'Runtime path' },
    { key: 'status', label: 'Status', filter: true },
  ]}
  rows={[
    { step: 'Step 1: Open Project', userAction: 'Open a repository or worktree in CastCodes.', runtimePath: 'Client selects a visible project context.', status: 'CastCodes-owned UI' },
    { step: 'Step 2: Launch Session', userAction: 'Type a Cast Code or task such as `:cody: fix failing tests`, then choose Codex or Claude Code (or let the client map the intent to a supported harness).', runtimePath: 'Cast Agent parses intent into a structured launch request; `POST /api/v1/sessions` with explicit project root, cwd, harness, prompt, and optional title.', status: 'Coven shipped API' },
    { step: 'Step 3: Watch Session', userAction: 'Keep the session visible in a pane/session row.', runtimePath: '`GET /api/v1/sessions` plus `GET /api/v1/events?sessionId=...`; live attach/input only after daemon liveness checks.', status: 'Coven shipped API, client UI stabilizing' },
    { step: 'Step 4: Inspect Files', userAction: 'Inspect changed files, terminal output, and diffs before accepting anything.', runtimePath: 'Client reads repo state and event history; Coven remains the session source of truth.', status: 'CastCodes-owned review UI' },
    { step: 'Step 5: Finish Loop', userAction: 'Merge locally, open a PR, archive the session, summon it later, or sacrifice it when logs should be deleted.', runtimePath: 'Client commands plus daemon-backed archive/summon/sacrifice lifecycle.', status: 'Mixed: client workflow plus Coven session lifecycle' },
  ]}
/>

The demo should not hide unavailable states. If Coven is missing, point to [Install Coven](/docs/guide/install). If the daemon is stopped, suggest `coven daemon start`. If a harness is missing or unauthenticated, show `coven doctor` and the provider-owned setup step. If the session has completed, failed, been killed, archived, or orphaned, show logs and diffs instead of live input controls.

What is shipped today:

- Coven CLI, daemon, `coven.daemon.v1` local API, Codex and Claude Code harness ids, sessions, events, input, kill, archive, summon, and sacrifice.
- Cast Agent as the CastCodes-side substrate and session backend shape.
- CastCodes as the first-party workspace direction for visible panes, files, diffs, and review.

What is still roadmap or hardening work:

- Treating every Cast Code example as stable syntax.
- Broad OpenClaw plugin usage before compatibility tests and API versioning gates stay green.
- Hosted, remote, browser, or mobile access to the raw daemon socket.

## Authority Rule

Cast Codes should never be documented as permission to bypass the daemon.

- **Who parses user intent?** A client, Cast Agent, or future parser can parse Cast Codes.
- **Who decides whether a session may launch?** The Coven daemon validates project root, cwd, and harness id.
- **Who decides whether live input or kill is allowed?** The Coven daemon validates session id and liveness.
- **Who owns external action approvals?** The daemon/control-plane policy layer must own approval requirements.
- **Can a client treat Cast Code syntax as permission?** No. Syntax is intent; daemon validation is authority.
