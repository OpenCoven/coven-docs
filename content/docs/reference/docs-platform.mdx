---
title: "Docs Platform"
description: "How Coven docs use Fumadocs components, search, exports, and validation."
---

# Docs Platform

Coven docs use Fumadocs as the documentation substrate: MDX components for richer pages, built-in search, export paths, link validation, and optional Obsidian import tooling.

<Steps>
  <Step>

  ### Author MDX

  Use standard MDX plus the shared Fumadocs component registry. Code fences render through the Fumadocs code block wrapper, and images use zoom automatically.

  </Step>
  <Step>

  ### Validate and build

  Run the Fumadocs platform check, Mermaid checks, link validation, and the Next build before publishing docs changes.

  </Step>
  <Step>

  ### Export when needed

  EPUB is available from \`/export/epub\`. PDF export runs against a local server so rendered pages, diagrams, and styles are captured.

  </Step>
</Steps>

## Tooling

<Tabs items={['Exports', 'Validation', 'Obsidian']} groupId="docs-platform">
  <Tab value="Exports">

  <Files>
    <Folder name="app" defaultOpen>
      <Folder name="export" defaultOpen>
        <Folder name="epub" defaultOpen>
          <File name="route.ts" />
        </Folder>
      </Folder>
    </Folder>
    <Folder name="scripts" defaultOpen>
      <File name="export-pdf.mjs" />
    </Folder>
  </Files>

  ~~~sh title="Export PDF"
  npm run export:pdf
  ~~~

  </Tab>
  <Tab value="Validation">

  ~~~sh title="Validate docs platform wiring"
  npm run check:fumadocs
  npm run check:links
  npm run build
  ~~~

  </Tab>
  <Tab value="Obsidian">

  ~~~sh title="Generate docs from a vault"
  OBSIDIAN_VAULT_DIR="../Coven Vault" npm run export:obsidian
  ~~~

  </Tab>
</Tabs>

## Runtime Fields

<TypeTable
  type={{
    searchTag: {
      description: 'The first docs slug segment used by the search filter.',
      type: 'string',
      default: 'guide | familiars | reference | openapi',
    },
    githubUrl: {
      description: 'The source URL shown in the page action popover.',
      type: 'string',
      required: true,
    },
    lastModifiedTime: {
      description: 'The latest GitHub edit timestamp for the current MDX source file.',
      type: 'Date | null',
    },
  }}
/>

<Accordions type="single">
  <Accordion title="Why Fumadocs components live in one registry">

  Keeping MDX component wiring in \`components/mdx-components.tsx\` makes docs pages predictable. Individual MDX files can use the same component set without repeating imports, while custom Coven components such as Mermaid and data tables stay available.

  </Accordion>
</Accordions>

## QA Checklist

<TypeTable
  type={{
    search: {
      description: 'Use the header search and section filter for install, API, harness, and OpenClaw queries. The search API lives at /api/search and supports guide, familiars, reference, and openapi tags.',
      type: 'UI + route check',
      required: true,
    },
    graph: {
      description: 'Open /docs and confirm the graph renders real docs nodes and follows internal links extracted from MDX content.',
      type: 'Rendered docs check',
      required: true,
    },
    mermaid: {
      description: 'Run Mermaid transform/normalization checks and verify the preview UI exposes renderer version, source copy, SVG copy, and fullscreen controls.',
      type: 'npm run check:mermaid && node scripts/check-mermaid-preview-ui.mjs',
      required: true,
    },
    epub: {
      description: 'Open /export/epub to verify the EPUB route responds with coven-docs.epub.',
      type: 'Route check',
      required: true,
    },
    pdf: {
      description: 'Run npm run export:pdf against a local docs server. Override DOCS_PDF_ROUTES when testing a focused page set.',
      type: 'Scripted browser export',
      required: true,
    },
    mobile: {
      description: 'Smoke the docs at a mobile viewport and confirm nav, tables, Mermaid diagrams, and code blocks remain reachable without horizontal page overflow.',
      type: 'Browser smoke',
      required: true,
    },
  }}
/>
